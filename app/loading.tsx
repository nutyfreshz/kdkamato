import { ProcessingOverlay } from "@/components/processing-overlay";

export default function Loading() {
  return <ProcessingOverlay title="กำลังโหลด..." detail="กำลังเปิดหน้าถัดไป กรุณารอสักครู่" />;
}
